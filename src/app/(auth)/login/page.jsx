import Login from "@/components/authComponents/Login";

export const metadata = {
  title: "Login — TileVista",
  description:
    "Sign in to your TileVista account to explore premium tile collections and access exclusive tile details.",
};

const LoginPage = () => {
  return <Login></Login>;
};

export default LoginPage;
